@model IEnumerable<bOOkStoreManagement.Models.Domain.Publisher>
@{
}

<div class="container pt-5">
    
    <div class="row">
        <div class="col-md-6">
            <h3 class="text-center">Publishers</h3>

            <table  class="table table-stripped table-bordered table-dark text-center">
                <thead>
                    <tr>
                        <th>PublisherName</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach(var item in Model){
                    <tr>
                        <td>@item.PublisherName</td>
                            <td class="d-flex justify-content-center">
                                <a class="btn btn-success  mx-1" href="/Publisher/Update?id=@item.Id"><i class="bi bi-pen"></i> Edit</a> |
                                <a class="btn btn-danger  mx-1" onclick="return window.confirm('Are you sure?')" href="/Publisher/Delete?id=@item.Id"><i class="bi bi-trash"></i> Delete</a>
                        </td>
                    </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

    <div class="row">
        <div class="col-md-6 text-center">
            <a class="btn btn-dark" href="/Publisher/Add">+ Add more</a>
        </div>
    </div>
</div>

